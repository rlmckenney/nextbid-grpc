{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {"dependsOn": ["^build"], "outputs": ["dist/**"]},
    "test": {"dependsOn": [], "outputs": []},
    "clean": {"cache": false},
    "check-types": {"outputs": []},
    "format": {"outputs": []},
    "lint": {"outputs": []}
  }
}
